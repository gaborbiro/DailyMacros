<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application>
        <activity
            android:name=".quickpickwidget.QuickPickWidgetConfigActivity"
            android:exported="true"
            android:theme="@style/Theme.Notes" />

        <receiver
            android:name=".diarywidget.DiaryWidgetReceiver"
            android:label="@string/widget_diary_name"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_info_diary" />
        </receiver>

        <receiver
            android:name=".quickpickwidget.QuickPickWidgetReceiver"
            android:label="@string/widget_quick_pick_name"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_info_quick_pick" />
        </receiver>

        <receiver
            android:name=".WidgetBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
