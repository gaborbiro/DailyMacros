<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!--
       Required for Google Play Billing. Play Console gates subscription/
       product creation on detecting this permission in an uploaded build,
       independently of whether the app already implements a purchase flow.
    -->
    <uses-permission android:name="com.android.vending.BILLING" />

    <!--
       Firebase Analytics transitively depends on play-services-measurement-api,
       which declares this permission (and its Android 13+ AdServices
       equivalent, ACCESS_ADSERVICES_AD_ID) in its own manifest so it can
       collect the advertising ID for ad-attribution features. This app
       doesn't use ads or ad personalisation, so both are stripped here to
       keep the Play Console "Advertising ID" data-safety answer ("No")
       accurate. Removing only the legacy permission isn't enough - Play's
       upload check also flags ACCESS_ADSERVICES_AD_ID as an advertising-ID
       permission.
    -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" tools:node="remove" />
    <uses-permission android:name="android.permission.ACCESS_ADSERVICES_AD_ID" tools:node="remove" />
    <!--
       READ_MEDIA_IMAGES / READ_EXTERNAL_STORAGE are intentionally NOT declared.
       The visible app adds meal photos via the Android Photo Picker
       (PickVisualMedia) and the share sheet (ACTION_SEND), neither of which
       needs a media permission. The only consumer of broad media access is the
       hidden auto photo-detection feature (PhotoMonitorWorker, gated by the
       auto_photo_recognition_enabled Remote Config flag). When that feature is
       shipped, re-add:
         <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
         <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
       and complete the Play Console "Photo and video permissions" declaration.
       See README.md → "Pending doc/text updates for hidden features".
    -->


    <application
        android:name=".App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true">

        <activity
            android:name=".features.main.MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.Notes"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>

<provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>
    </application>
</manifest>
